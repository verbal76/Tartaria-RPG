# ledger

One file per HAL promotion request: `promote-hal-<full-40-hex-sha>.json` with `{ "target": "hal", "sha": "<the same sha>" }`. Append-only. A record that exists here, or ever existed in this branch history, is consumed. See ../README.md.
