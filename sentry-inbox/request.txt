sync 2026-08-27 — walk the ISSUE feed, because the project feed caps at 100.

THE VERDICT BLOCK'S FIRST RUN LIED, AND IT CAUGHT ITSELF DOING IT:

  1 page(s), 100 rows — COMPLETE — cursor exhausted
  ⚠ MISSING parts are therefore ABSENT FROM SENTRY

Exactly 100 rows, one page, no next cursor offered — while this tree already
holds 256 synced event directories. Sentry plainly has more than a hundred. The
legacy /projects/.../events/ feed returns the newest 100 and declines to
paginate past them, reporting "no next page" in exactly the way a genuinely
exhausted listing would. Acting on that would have meant asking the owner to
re-send logs already sitting in Sentry.

TWO CHANGES:
1. A listing that ends ON a full page is now SUSPECT, never COMPLETE. Only one
   that ends SHORT of its page size has actually run out.
2. The issue feed is walked as well — /organizations/.../issues/{id}/events/
   follows a real cursor to the end. Project feed for recency, issue feed for
   depth, unioned by event id.

Expected: "N events after walking M issues" with N far above 100, and
mtaxtx2bzl8l (Great Scott, currently 5/27) and mtawqdrsm78g (3/23) filling in
without another send.
