sync 2026-08-27 — make the reader say which case it is, instead of guessing again.

TWO THINGS THIS RUN SETTLES.

1. SAFE FIELDS IS SET AND DID NOT WORK. The last run read back
   safeFields: ['chunkBlocks'], and the four logs sent AFTER that save were still
   scrubbed — 4 redactions of ~344 chars each, same trigger positions. Safe
   Fields excludes by field NAME; the slice is an ARRAY, so the rule runs on
   chunkBlocks.0, .1, .2 and the parent's exemption does not follow it down. The
   relay no longer promises "redactions should be zero from here on"; it reports
   what is set and defers the outcome to the measurement.

2. THE MISSING PARTS — ABSENT, OR UNREAD? The pagination fix recovered nothing,
   and that run could not distinguish the two cases: whether the events are gone
   from Sentry or the reader stopped asking, a re-sync produces zero new files
   either way. So the run now ends with a VERDICT block stating how many pages
   were walked, how many rows came back, and whether the cursor was exhausted
   (COMPLETE — the parts are genuinely absent and only a fresh send recovers
   them) or capped (TRUNCATED — they may simply be unread).

The owner's character is Great Scott and his bundle is mtaxtx2bzl8l at 5/27.
Whether he needs to send again turns entirely on that one word.
