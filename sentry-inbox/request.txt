sync 2026-08-27 — three more logs sent, first pull after OTA-1521 + 1522.

What to look for in these, in order:

1. PHANTOM DEATHS. OTA-1521 makes the OTA reload mark its own orderly exit, so a
   deliberate restart should stop minting `PROCESS KILLED — no JS ran` records.
   Eight of the previous eighteen followed "ota: Restarting to apply..." by 4s to
   7s. If the fix works, deaths recorded AFTER an OTA apply disappear and only
   the ten genuinely unexplained ones remain — which is the whole of #81.

2. THE STUN. OTA-1522 makes the incapacitation gate read the post-tick status
   list. "stunned fades." should never again be followed a millisecond later by
   "You cannot move. Your action is lost."

3. mtaxtx2bzl8l, STILL 5/27 AND NOW STRANGE. The device logged
   `DELIVERED (inline, no attachments): beacon out, 27/27 parts accepted
   (404834 chars)` at 03:03:03 — accepted AND flush-confirmed — and Sentry holds
   five parts. Every earlier failure in this hunt was honest about itself; this
   one is not. The issue walk now runs clean and every listing ends short of a
   full page, so "unread" is no longer an available excuse. Watch whether the new
   sends land whole.
