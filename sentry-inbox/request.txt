sync 2026-08-27 — the issue walk was 400ing. Fixed; go get the missing parts.

VERDICT.txt named it in one line, which is exactly why it now reports failed
fetches:

  issue list FAILED for tartaria: HTTP Error 400: Bad Request

`statsPeriod=90d` is rejected by the project-issues endpoint — it accepts only
'', '24h' or '14d'. So the walk added to read past the project feed's 100-row cap
never ran at all, while the verdict showed a single capped listing and no sign
anything was missing.

Two parameters now, both load-bearing:
  · statsPeriod=  (empty) — anything else risks the same 400.
  · query=        (empty) — the default is `is:unresolved`, which would silently
    skip every resolved issue and take its whole event history with it.

Expected: the fetch-problems block reads "every fetch this run succeeded", the
issues path appears in the page audit with real page counts, and mtaxtx2bzl8l
(Great Scott, 5/27) and mtawqdrsm78g (3/23) fill in — with no new send.
