sync 2026-08-27 — follow the pagination cursor and go get Great Scott's missing
parts. They were never lost; this reader stopped asking for them.

THE OWNER'S CHARACTER IS GREAT SCOTT, and his bundle is the one that came back
5/27. The 03:0x sync fetched EXACTLY 100 event directories — Sentry's events
endpoint caps a page at 100 and paginates with a `Link: rel="next"` cursor this
reader never read. His four sends were about 120 events, and his character's
bundle went out FIRST, so its parts were the oldest and fell off page one:

  mtaxtx2bzl8l: 5/27 — have 20,21,22,23,26, missing 1-19, 24, 25, 27

Late parts present, early parts absent. That is backwards for a send dying on the
device and is the exact signature of a truncated page. mtawqdrsm78g at 3/23 is
the same wound. Both were reported as MISSING, which reads like a device fault
and sent the hunt in the wrong direction.

Expected from this run: `tartaria: N events across all pages` with N well over
100, mtaxtx2bzl8l assembling at or near 27/27, mtawqdrsm78g likewise — and the
sixty tiles of travel finally readable.

If the page cap is ever reached the run now says so loudly instead of returning a
short list that looks complete.
