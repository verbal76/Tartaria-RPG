sync 2026-08-27 — read the scrubbing config back cleanly, and prove the rebase
fix works.

THE BLOCKS FIX IS CONFIRMED ON THE OWNER'S OWN DEVICES. All three new bundles
report `session start: 2026-08-27-1520`, so they were sent on the array shape,
and the cost per bundle collapsed:

  mtawqnivyk0q (1519)  5 redactions  74,999 chars
  mtaxu4o1qm9m (1520)  4 redactions   1,498 chars
  mtawr2axtwkc (1519)  4 redactions  60,000 chars
  mtaxuqrhd0b3 (1520)  4 redactions   1,376 chars

Same rule, same trigger positions (parts 3/9/10/11), 43x less taken — and about
61,000 more characters of log per send arriving intact. Travel came back with it:
27 cardinal steps in a bundle that previously showed zero.

BUT @password:filter IS STILL FIRING, so the Safe Fields entry did not stop it.
That is the answer to the open question: a name-based exclusion does not reach
into an array's children. This run reads `safeFields` back to confirm the setting
is genuinely saved, so the conclusion rests on config rather than inference.

Also fixed here: the relay pushed without rebasing and lost races against the
session. Runs 29 and 31 fetched everything, committed it, then threw the run away
on `! [rejected] (fetch first)`. It now rebases and retries up to five times.
