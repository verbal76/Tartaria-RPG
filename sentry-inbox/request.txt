sync 2026-08-27 — pull everything, and make the relay confess its own failures.

Last run's VERDICT.txt showed only the project-events listing in the page audit.
The issue-feed walk added to reach past that feed's 100-row cap does not appear
at all, which means it threw and the failure went nowhere but a buried print. A
fallback that fails silently is a fallback believed in for no reason.

So fetch_notes now collects every failed fetch and VERDICT.txt states them up
front. If the issue walk is broken, this run says so in one line instead of
leaving a capped listing looking like the whole story.

Still open: mtaxtx2bzl8l (Great Scott) at 5/27 and mtawqdrsm78g at 3/23. Neither
has been PROVEN absent from Sentry — the only listing that has run is one that
ends on a full page, which proves nothing.
