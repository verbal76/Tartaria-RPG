sync 2026-08-27 — the owner added `chunkBlocks` to the tartaria project's Safe
Fields. This run reads the config back to confirm it saved.

What this sync CAN prove: that `safeFields` now contains chunkBlocks. The config
probe prints it, and the exemption line flips to YES.

What it CANNOT prove: that the exemption actually stops @password:filter. Safe
Fields excludes by FIELD NAME, and OTA-1520 sends the slice as an ARRAY — the
scrubber walks into chunkBlocks.0, chunkBlocks.1 and so on, and whether a
name-based exclusion reaches an array's children is exactly the thing being
tested. Scrubbing happens at ingest, so only a send made AFTER the setting was
saved, from a device running 1520, can answer it.

The verdict will read off the SCRUB TOTALS block on that next sync: zero
redactions on a fresh bundle means it took; @password:filter still taking ~400
chars a hit means the name-based exclusion stops at the array and the only
remaining lever is the "Use Default Scrubbers" toggle.
