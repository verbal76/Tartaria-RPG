// GamepadNav (native stub) — the real implementation is GamepadNav.web.tsx, which
// drives on-screen button navigation from a controller on PC/Steam Deck. On
// phones there's a touchscreen, so this renders nothing. Metro resolves this file
// on android/ios and the .web.tsx file on web.

export function GamepadNav(): null {
  return null;
}
